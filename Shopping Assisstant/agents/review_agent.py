class ReviewAgent:
    def add_reviews(self, products):
        # The Fake Store API already includes rating fields, so just return as-is.
        # If external review processing needed, insert here.
        return products
